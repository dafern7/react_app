//just to catch all other links

import React from "react";
import "./NotFound.css"

export default function(NotFound) {
    return(
        <div className="NotFound">
            <h2> 404: Page Not Found </h2>
        </div>
    );
}